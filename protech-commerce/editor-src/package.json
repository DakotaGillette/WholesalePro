{
	"name": "protech-commerce-editor",
	"private": true,
	"version": "1.0.0",
	"description": "The client-side email template editor. Built output is committed to ../assets/editor/; this folder is dev-only and never deployed.",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"test": "vitest run",
		"test:watch": "vitest",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"preact": "10.24.3"
	},
	"devDependencies": {
		"@preact/preset-vite": "2.9.3",
		"@types/node": "22.10.2",
		"typescript": "5.6.3",
		"vite": "5.4.11",
		"vitest": "2.1.8"
	}
}
